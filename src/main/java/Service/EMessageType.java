package Service;

public enum EMessageType
{
    USER_NAME,
    USER_NAME_RESPONSE,
    USER_LIST,
    USER_ON,
    USER_OFF,

    USER_LEAVE,
    USER_DISCONNECT,

    DATAFILE,
    CSV,
    TEXT,
    IMAGE,
    VIDEO,
}
